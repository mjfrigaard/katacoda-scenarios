---
title: "Katacoda scenarios"
output: 
  html_document: 
    toc_depth: 6
    highlight: textmate
    keep_md: yes
---

I will be creating three scenarios for using R in Katacoda. They are listed below:

## Scenario 1: Format and shape your data with R in the tidyverse

### intro 

This introduces the `tidyverse` and gives a little preview of what is to come. 

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/intro.png")
```

### step 1

Step 1 covers how to install and load packages in R (`install.packages()` and `library()`). It also covers some basic information about functions.

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step1.png")
```

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/00-name-arg.png")
```

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/00-position-arg.png")
```

### step 2

This covers the Bob Ross dataset from 538, and using the `BobRoss <- fivethirtyeight::bob_ross` function to create a dataset in R. 

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step2.png")
```

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/00-538-bob-ross.png")
```


### step 3

Here we cover loading data into R from the `fivethirtyeight` package with `tibble::tribble()`.

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step3.png")
```

### step 4

This introduces `tidyr` and `dplyr::glimpse()`. 

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step4.png")
```

### step 5

We create a tidy dataset with `pivot_longer()`.

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step5.png")
```

```{r , echo=FALSE, fig.height=3, fig.width=5}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/01-orig-bob-ross.png")
```

```{r , echo=FALSE, fig.height=3, fig.width=5}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/02-long-bob-ross.png")
```

### step 6

We revert to the wide dataset with `tidyr::pivot_wider()`

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step6.png")
```

### step 7

We split a column with `tidyr::separate()`

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step7.png")
```


### step 8

We bring two columns back together with `tidyr::unite()`

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step8.png")
```

### step 9

We change the format of a variable with `dplyr::mutate()`

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step9.png")
```

```{r , echo=FALSE, fig.height=3, fig.width=5}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/03-original-data.png")
```

```{r , echo=FALSE, fig.height=3, fig.width=5}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/04-pivot-longer.png")
```

```{r , echo=FALSE, fig.height=3, fig.width=5}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/05-pivot-wider.png")
```

### step 10

We create a new variable based on a condition with `dplyr::if_else()` and `dplyr::mutate()`.

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step10.png")
```

### step 11

We create a new variable based on multiple conditions with `dplyr::case_when()` and `dplyr::mutate()`. 

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step11.png")
```

### step 12

We combine creating new variables with reshaping to check our work (`dplyr::case_when()` and `dplyr::mutate()`, then `dplyr::count`, and `tidyr::pivot_wider()`).

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/step12.png")
```

### finish

Wrap up. 

```{r , echo=FALSE}
# fs::dir_ls("figs")
knitr::include_graphics(path = "figs/finish.png")
```

***

Coming soon!

*Scenario 2: Build beautiful, customized graphs and charts in R with ggplot2*

*Scenario 3: Combine narrative with numbers for effective storytelling in R*

This is the `README.Rmd` file for the Katacoda scenarios. All the scenarios and courses can be found on my profile page [here](https://katacoda.com/mjfrigaard).

